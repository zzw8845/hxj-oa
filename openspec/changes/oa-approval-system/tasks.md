## 1. 后端工程搭建

- [x] 1.1 更新 pom.xml 引入 Spring Boot 3.2、Spring Web、Spring Data JPA、Spring Security、JWT、MySQL、Flyway、Flowable、SpringDoc OpenAPI、EasyExcel 依赖，验证 `mvn compile` 通过
- [x] 1.2 创建应用主类与基础配置（application.yml、CORS、全局异常处理），验证应用可启动
- [x] 1.3 配置 MySQL 数据源与 Flyway 迁移脚本目录，验证 `mvn spring-boot:run` 启动且迁移执行成功

## 2. 数据模型与持久层

- [x] 2.1 实现用户、角色、权限点、数据范围实体及关联，验证 JPA 单元测试通过
- [x] 2.2 实现单据实体（三类业务类型、所属公司、对应项目、金额、发票明细、付款后置材料标记、合同编号、状态、当前节点、前置关联、流程实例 ID、风险标记）与附件实体，验证 JPA 单元测试通过
- [x] 2.3 实现审批记录实体、抄送记录实体、归档台账实体与流程配置实体（节点链、条件分支规则），验证 JPA 单元测试通过
- [x] 2.4 实现单据编号生成器（类型前缀+日期+流水号），验证并发唯一性测试通过
- [x] 2.5 编写 Flyway 初始迁移脚本与种子数据，验证启动后数据可查询：
  - 19 个预置角色（超级管理员/公司领导/执行总经理/一级中心负责人/二级部门负责人/会计主管&内控/核算会计/内控主管/内控专员/法务/法务主管/行政专员/行政主管/商务专员/运营服务主管/交付主管/财务经理/出纳主管/出纳），含部门、岗位、数据范围、权限点、成员
  - 20 条业务单据流程配置（闭店/采购申请/低值易耗品领用/非标合同审批及用印/费用报销/费用预算/应付款申请/公司发文申请/固定资产报废报损/固定资产调拨/固定资产入库/黄金业务开户/借款申请/客户交易手续费调整/客户结算服务费/提货人新增或变更/通用审批申请/业务招待申请/银行账户管理调整/用印及证照申请），含节点链与条件分支
  - 快捷单据目录（日常付款 37 项、业务付款 9 项、用印申请 3 项）
  - 示例人员与示例单据

## 3. 认证与权限模块

- [x] 3.1 实现登录接口（账号+密码校验、JWT 签发），验证登录成功/失败测试通过
- [x] 3.2 实现 JWT 过滤器与 Spring Security 配置，验证受保护接口未带 token 返回 401
- [x] 3.3 实现基于角色的权限点与数据范围校验（本人/本部门/全部），验证权限控制测试通过
- [x] 3.4 实现员工账号创建/编辑接口（含账号密码必填校验、员工信息编辑、在职/离职状态管理），验证创建与校验测试通过
- [x] 3.5 实现角色管理接口（新增、修改角色，含权限点与数据范围），验证角色配置测试通过
- [x] 3.6 实现部门角色架构查询接口（按部门树形返回角色、岗位、权限），验证树形结构测试通过

## 4. 单据管理 API

- [x] 4.1 实现单据创建/提交接口（三类单据字段校验、编号生成、状态初始化、自选抄送人、启动流程实例），验证提交测试通过
- [x] 4.2 实现单据分类规则（合作方退款/服务费结算/闭店/退店 → 业务付款，其余 → 日常付款），验证分类测试通过
- [x] 4.3 实现单据查询接口（关键字、状态、业务类型、单据类型、申请人组合筛选），验证筛选测试通过
- [x] 4.4 实现单据详情接口（含流程节点、附件、审批记录、前置关联、抄送记录），验证详情测试通过
- [x] 4.5 实现前置单据关联接口（报销按单据编号、业务付款按合同编号查询已审批单据并关联），验证关联测试通过
- [x] 4.6 实现单据列表权限过滤（按数据范围），验证普通用户/部门负责人/超管可见性测试通过
- [x] 4.7 实现快捷单据目录接口（按业务类型返回可快捷提交的单据列表），验证目录返回测试通过
- [x] 4.8 实现再次提交接口（复制历史单据信息生成新编号），验证再次提交测试通过
- [x] 4.9 实现附件上传/下载/预览接口（文件存储+元数据记录+按业务类型生成必传附件清单），验证上传下载测试通过

## 5. 审批流引擎（Flowable）与审批操作

- [x] 5.1 集成 Flowable 引擎并配置数据源，验证引擎启动与流程定义部署成功
- [x] 5.2 定义 BPMN 流程模板（线性节点链 + 排他网关条件分支 + 抄送节点），验证流程定义解析与部署测试通过
- [x] 5.3 实现流程服务封装（启动流程、查询待办任务、完成任务、条件分支判断、抄送处理），验证流程流转测试通过
- [x] 5.4 实现通过审批接口（凭证上传、意见记录、任务完成、末节点完结归档），验证通过测试通过
- [x] 5.5 实现指定层级驳回接口（驳回层级、原因必填、需补充材料选填、状态更新、流程跳转），验证驳回测试通过
- [x] 5.6 实现通过但补材料接口（付款前/付款后模式、补充对象与材料），验证补材料测试通过
- [x] 5.7 实现加签接口（加签人员必选、加签任务创建），验证加签测试通过
- [x] 5.8 实现审批权限校验（仅当前任务审批人或超管可操作），验证非审批人操作被拒绝测试通过
- [x] 5.9 实现流程历史查询接口（Flowable 历史任务与业务审批记录合并），验证历史留痕测试通过
- [x] 5.10 实现抄送节点处理（抄送相关角色/人员，记录抄送记录），验证抄送测试通过
- [x] 5.11 实现流程配置管理接口（流程配置列表/详情查询、新增流程、修改流程节点链与条件分支，仅管理员可操作，修改后新提交单据按新流程流转），验证流程配置 CRUD 测试通过
- [x] 5.12 实现流程配置可视化链条数据接口（按业务单据类型返回完整节点链，供"流程管理"页面展示），验证链条数据测试通过

## 6. 用印管理

- [x] 6.1 实现用印申请创建接口（独立字段、无金额、独立流程定义），验证用印申请测试通过
- [x] 6.2 实现用印凭证回传与归档接口（未回传不可完结），验证回传归档测试通过

## 7. 归档台账

- [x] 7.1 实现归档查询接口（仅已通过单据、多条件组合查询），验证归档查询测试通过
- [x] 7.2 实现台账导出接口（Excel/CSV），验证导出文件内容测试通过

## 8. 统计看板与风险预警

- [x] 8.1 实现首页统计接口（已驳回/待我审批/本月已办结数量及环比、流程合规率、菜单角标数量），验证统计测试通过
- [x] 8.2 实现首页待办审批列表接口（按临近超时优先排序），验证待办列表测试通过
- [x] 8.3 实现工作看板统计接口（申请总量/审批中/已办结/平均审批时长/节点处理效率/状态分布），验证看板统计测试通过
- [x] 8.4 实现近7天流程趋势接口（每日发起/办结数量、状态分布），验证趋势测试通过
- [x] 8.5 实现风险预警接口（金额 ≥ 8 万元标记为风险单据，返回风险列表与数量），验证风险识别测试通过

## 9. 接口文档与交付

- [x] 9.1 配置 SpringDoc OpenAPI 并编写接口注解（单据、审批、角色、归档、统计等全部 API），验证 Swagger UI 可访问且接口描述完整
- [x] 9.2 编写 API 对接说明文档（认证方式、接口清单、错误码、示例请求/响应），验证文档覆盖全部接口
- [x] 9.3 运行后端全部单元测试，验证 `mvn test` 通过
- [x] 9.4 端到端验证：报销申请全链路（提交→直属主管→采购询价→会计主管&内控→采购办理→发起人签收→归档），条件分支正确跳过"执行总经理（≥2万元）"节点
- [x] 9.5 用印申请独立条线端到端验证（提交→审批→盖章文件闭环阻断→回传盖章文件→完结）
- [x] 9.6 角色权限端到端验证（shufei JWT 含正确角色/权限/数据范围，dashboard 数据范围过滤正确：风险数=0 vs linanran=1）
- [x] 9.7 统计看板与风险预警端到端验证（home/board/trend/risks 全部正常，今日提交6个/完成3个，风险单据1个）
